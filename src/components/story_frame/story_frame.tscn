[gd_scene load_steps=13 format=2]

[ext_resource path="res://assets/gigasprite.png" type="Texture" id=1]
[ext_resource path="res://theme.tres" type="Theme" id=2]
[ext_resource path="res://assets/chip.png" type="Texture" id=3]
[ext_resource path="res://components/story_frame/story_frame.gd" type="Script" id=4]
[ext_resource path="res://assets/beeper.png" type="Texture" id=5]
[ext_resource path="res://assets/font/Rubik-BoldItalic.ttf" type="DynamicFontData" id=6]
[ext_resource path="res://assets/patch.png" type="Texture" id=7]
[ext_resource path="res://assets/font/NotoSansSC-Regular.otf" type="DynamicFontData" id=8]

[sub_resource type="StyleBoxFlat" id=2]
content_margin_left = 16.0
content_margin_right = 16.0
content_margin_top = 16.0
content_margin_bottom = 16.0
bg_color = Color( 0, 0, 0, 1 )
border_width_left = 1
border_width_top = 1
border_width_right = 1
border_width_bottom = 1
border_color = Color( 1, 1, 1, 1 )

[sub_resource type="DynamicFont" id=10]
size = 24
outline_color = Color( 0, 0, 0, 1 )
font_data = ExtResource( 6 )
fallback/0 = ExtResource( 8 )

[sub_resource type="Shader" id=8]
code = "/*
Shader from Godot Shaders - the free shader library.
godotshaders.com/shader/waveforms

This shader is under CC0 license. Feel free to use, improve and 
change this shader according to your needs and consider sharing 
the modified result to godotshaders.com.
*/

shader_type canvas_item;
render_mode blend_premul_alpha;

uniform int lines = 80; // Amount of lines the waveform is build up of
uniform float amplitude : hint_range(0.0, 1.0) = 0.9; // Height of the wave
uniform float frequency : hint_range(0.0, 0.5) = 0.1; // Set a lower value for a smoother wave
uniform float intensity : hint_range(0.0, 1.0) = 0.75; // Lower values creates gaps in the wave while higher make the wave more solid
uniform float line_sharpness : hint_range(0.0, 1.0) = 0.5; // Fuzzy edges on each line
uniform float line_size : hint_range(0.0, 1.0) = 0.8; // Thickness of the lines
uniform float fade : hint_range(0.0, 0.5) = 0.1; // Blurres the top and bottom of the lines
uniform float rest_size : hint_range(-2., 2.) = -0.2; // The size of the lines when the line is not animating, i.e at value 0. Tweak if Fade is used.
uniform float edge : hint_range(0.0, 0.5) = 0.05; // How close to the edges should the animation beginn
uniform float speed = 0.5; // Speed of wave animation
uniform vec4 color : hint_color = vec4(.8, 0.25, 0.5, 1.0); // Color of wave
uniform bool dot_matrix = false; 
uniform int dot_matriz_size = 80;

uniform bool speech_sim = true; // Toggle to simulate speach. Will create a more erratic movement in the wave
uniform float speech_intensity : hint_range(0.0, 2.0) = 0.8; // How fast will the speech movement be
uniform float progress : hint_range(0.0, 1.0) = 1.0; // Use to turn the animation on and off from outside the shader. 0 - off, 1 - on. Can animate values between which will shrink the wave.

float random(vec2 uv) {
	return fract(sin(dot(uv.xy,
	vec2(12.9898,78.233))) * 43758.5453123);
}

float noise(vec2 uv) {
	vec2 uv_index = floor(uv);
	vec2 uv_fract = fract(uv);

	// Four corners in 2D of a tile
	float a = random(uv_index);
	float b = random(uv_index + vec2(1.0, 0.0));
	float c = random(uv_index + vec2(0.0, 1.0));
	float d = random(uv_index + vec2(1.0, 1.0));

	vec2 blur = smoothstep(0.0, 1.0, uv_fract);

	return mix(a, b, blur.x) +
				(c - a) * blur.y * (1.0 - blur.x) +
				(d - b) * blur.x * blur.y;
}

float fbm(vec2 uv) {
	int octaves = 4;
	float amp = 0.5;
	float freq = 4.;
	float value = 0.0;
	
	for(int i = 0; i < octaves; i++) {
		value += amp * noise(freq * uv - 0.5);
		amp *= 0.6;
		freq *= 2.;
	}
	return value;
}


void fragment()
{
	// Some initialising
	float time = TIME;
	vec4 c = vec4(vec3(0.0), 1.0);
	float f_columns = float(lines);
	
	// Make some distance to the edges before the animation starts
	float cutoff = smoothstep(0.0, edge, (round ((UV.x) * (f_columns) ) / f_columns) );
	cutoff *= 1. - smoothstep(1.-edge, 1.0, (round ((UV.x) * (f_columns) ) / f_columns) );

	// Speech simulation setup
	float ss = 1.0;
	if (speech_sim == true){
		ss = fbm(vec2(1.0, time * speed * 1.3) * speech_intensity) * 1.5;
	}
	
	// Create the noise that controlls the animation
	float noise = fbm(vec2( round( (UV.x) * f_columns ) * frequency, time * speed) ) * ss * progress;
	noise *= cutoff; // Apply edge cutoff
	
	// Make wave values based on the noise
	float wave = smoothstep(1.-intensity, 1.0, noise) * amplitude;
	wave = wave + (rest_size * 0.2);
	
	// Create the lines
	float line = abs( sin(( f_columns * 3.1416 * UV.x) + 1.5) );
	line = smoothstep(1.-line_size, (1.-line_size) + (1.-line_sharpness), line);
	
	// Simulate dot_matrix
	if (dot_matrix){
		float dm = abs( sin(( float(dot_matriz_size) * 3.1416 * UV.y) + 1.5) );
		dm = smoothstep(1.-line_size, (1.-line_size) + (1.-line_sharpness), dm);
		line *= dm;
	}
	
	// Duplicate mask on top an bottom and apply to line
	float mask = 1.0 - smoothstep(wave, wave + fade, abs(UV.y - 0.5) * 2.);
	line *= mask;
	
	c = vec4(line) * color;
	
	COLOR = c;
}"

[sub_resource type="ShaderMaterial" id=9]
shader = SubResource( 8 )
shader_param/lines = 80
shader_param/amplitude = 1.0
shader_param/frequency = 0.5
shader_param/intensity = 1.0
shader_param/line_sharpness = 1.0
shader_param/line_size = 0.4
shader_param/fade = 0.0
shader_param/rest_size = -0.3
shader_param/edge = 0.25
shader_param/speed = 0.15
shader_param/color = Color( 1, 1, 1, 1 )
shader_param/dot_matrix = false
shader_param/dot_matriz_size = 10
shader_param/speech_sim = true
shader_param/speech_intensity = 0.75
shader_param/progress = 1.0

[node name="StoryFrame" type="PanelContainer"]
margin_top = 301.0
margin_right = 600.0
margin_bottom = 366.0
rect_min_size = Vector2( 500, 0 )
theme = ExtResource( 2 )
custom_styles/panel = SubResource( 2 )
script = ExtResource( 4 )

[node name="Chip" type="Control" parent="."]
visible = false
show_behind_parent = true
margin_left = 16.0
margin_top = 16.0
margin_right = 584.0
margin_bottom = 49.0

[node name="Label" type="Label" parent="Chip"]
margin_top = -38.0
margin_right = 92.0
margin_bottom = -9.0
custom_colors/font_color = Color( 1, 1, 1, 1 )
custom_fonts/font = SubResource( 10 )
text = "T_CHIP"
uppercase = true

[node name="Icon" type="TextureRect" parent="Chip"]
anchor_left = 1.0
anchor_right = 1.0
margin_left = -120.0
margin_top = -120.0
rect_min_size = Vector2( 100, 100 )
mouse_filter = 2
texture = ExtResource( 3 )
expand = true

[node name="Icon" type="TextureRect" parent="Chip/Icon"]
modulate = Color( 0.560784, 0.560784, 0.560784, 1 )
show_behind_parent = true
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
margin_left = -59.0
margin_top = -59.0
margin_right = 61.0
margin_bottom = 61.0
rect_min_size = Vector2( 100, 100 )
mouse_filter = 2
texture = ExtResource( 3 )
expand = true

[node name="Patch" type="Control" parent="."]
visible = false
show_behind_parent = true
margin_left = 16.0
margin_top = 16.0
margin_right = 584.0
margin_bottom = 49.0

[node name="Label" type="Label" parent="Patch"]
margin_top = -38.0
margin_right = 116.0
margin_bottom = -9.0
custom_colors/font_color = Color( 1, 1, 1, 1 )
custom_fonts/font = SubResource( 10 )
text = "T_PATCH"
uppercase = true
__meta__ = {
"_edit_use_anchors_": false
}

[node name="Icon" type="TextureRect" parent="Patch"]
anchor_left = 1.0
anchor_right = 1.0
margin_left = -120.0
margin_top = -120.0
rect_min_size = Vector2( 120, 120 )
mouse_filter = 2
texture = ExtResource( 7 )
expand = true
stretch_mode = 1

[node name="Icon" type="TextureRect" parent="Patch/Icon"]
modulate = Color( 0.560784, 0.560784, 0.560784, 1 )
show_behind_parent = true
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
margin_left = -59.0
margin_top = -59.0
margin_right = 61.0
margin_bottom = 61.0
rect_min_size = Vector2( 100, 100 )
mouse_filter = 2
texture = ExtResource( 7 )
expand = true

[node name="Beeper" type="Control" parent="."]
visible = false
show_behind_parent = true
margin_left = 16.0
margin_top = 16.0
margin_right = 584.0
margin_bottom = 49.0

[node name="Label" type="Label" parent="Beeper"]
margin_top = -38.0
margin_right = 128.0
margin_bottom = -9.0
custom_colors/font_color = Color( 1, 1, 1, 1 )
custom_fonts/font = SubResource( 10 )
text = "T_BEEPER"
uppercase = true

[node name="Icon" type="TextureRect" parent="Beeper"]
anchor_left = 1.0
anchor_right = 1.0
margin_left = -120.0
margin_top = -120.0
rect_min_size = Vector2( 120, 120 )
mouse_filter = 2
texture = ExtResource( 5 )
expand = true
stretch_mode = 1

[node name="Icon" type="TextureRect" parent="Beeper/Icon"]
modulate = Color( 0.560784, 0.560784, 0.560784, 1 )
show_behind_parent = true
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
margin_left = -59.0
margin_top = -59.0
margin_right = 61.0
margin_bottom = 61.0
rect_min_size = Vector2( 100, 100 )
mouse_filter = 2
texture = ExtResource( 5 )
expand = true

[node name="Gigasprite" type="Control" parent="."]
visible = false
show_behind_parent = true
margin_left = 16.0
margin_top = 16.0
margin_right = 584.0
margin_bottom = 49.0

[node name="Label" type="Label" parent="Gigasprite"]
margin_top = -38.0
margin_right = 179.0
margin_bottom = -9.0
custom_colors/font_color = Color( 1, 1, 1, 1 )
custom_fonts/font = SubResource( 10 )
text = "T_GIGASPRITE"
uppercase = true

[node name="Icon" type="TextureRect" parent="Gigasprite"]
anchor_left = 1.0
anchor_right = 1.0
margin_left = -120.0
margin_top = -111.0
margin_bottom = 9.0
rect_min_size = Vector2( 120, 120 )
mouse_filter = 2
texture = ExtResource( 1 )
expand = true
stretch_mode = 1

[node name="Icon" type="TextureRect" parent="Gigasprite/Icon"]
modulate = Color( 0.560784, 0.560784, 0.560784, 1 )
show_behind_parent = true
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
margin_left = -59.0
margin_top = -59.0
margin_right = 61.0
margin_bottom = 61.0
rect_min_size = Vector2( 100, 100 )
mouse_filter = 2
texture = ExtResource( 1 )
expand = true

[node name="Waveform" type="Control" parent="."]
show_behind_parent = true
margin_left = 16.0
margin_top = 16.0
margin_right = 584.0
margin_bottom = 49.0

[node name="Texture" type="ColorRect" parent="Waveform"]
material = SubResource( 9 )
anchor_left = 0.5
anchor_right = 0.5
margin_left = -150.0
margin_top = -39.0
margin_right = 150.0
margin_bottom = 8.0
rect_min_size = Vector2( 300, 0 )
size_flags_horizontal = 0
__meta__ = {
"_edit_use_anchors_": false
}

[node name="Tween" type="Tween" parent="."]

[node name="Voice" type="AudioStreamPlayer" parent="."]

[connection signal="gui_input" from="." to="." method="_on_StoryFrame_gui_input"]
[connection signal="script_changed" from="." to="." method="update"]
